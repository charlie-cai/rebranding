import SwiftUI

enum GreenLiteRebrandingLiteColors: String, CaseIterable {
<% it.rebranding.semantics.forEach(function(semantic){ %>
  case <%= it.func.camelCaseFromHyphenCase(semantic.name) %> = "<%= semantic.name.toLowerCase() %>"
<% }) %>
  case unknown = "unknown"
}

extension GreenLiteRebrandingLiteColors {
  public var uiColor: UIColor {
    UIColor.named(self.rawValue)
  }

  public var color: Color {
    Color(self.rawValue, bundle: bundle)
  }
}
